interface IMailConfig {
  driver: 'ethereal' | 'ses';
  defaults: {
    from: {
      email: string;
      name: string;
    };
  };
}

export default {
  driver: process.env.MAIL_DRIVER || 'ethereal',

  defaults: {
    from: {
      email: process.env.PRODUCTION_MAIL_EMAIL,
      name: process.env.PRODUCTION_MAIL_NAME,
    },
  },
} as IMailConfig;
